
@inject UserManager<UserIdentity> userManager
@if (signInManager.IsSignedIn(User))
{
    var u = await userManager.GetUserAsync(User);
    var id = u.Id;
    <li class="nav-item"><a href="/Blogs/WriteBlog" class="nav-link">DeğerlendirmeYaz</a></li>
    <li class="nav-item"><a href="/user/profile/@id" class="nav-link" title="Profilim">Profil</a></li>
    if (User.IsInRole("Admin"))
    {
        <li class="nav-item"><a href="/Editor/" class="nav-link" title="Editor Sayfası">Editor</a></li>
        <li class="nav-item"><a href="/Administration/" class="nav-link" title="Admin sayfası">Admin</a></li>
    }
    else if (User.IsInRole("Editor"))
    {
        <li class="nav-item"><a href="/Editor/" class="nav-link" title="Editor Sayfası">Editor</a></li>
    }

<li class="nav-item">
    <a class="nav-link" asp-area="Identity" asp-page="/Account/Manage/Index" title="Profili Yönet">Yönet</a>
</li>
        <form id="logoutForm" class="nav-item" asp-area="Identity" asp-page="/Account/Logout" asp-route-returnUrl="@Url.Action("Index", "Home", new { area = "" })">
            <button id="logout" class="nav-link border-0" style="background-color:transparent;">Çıkış</button>
        </form>
}
else
{
    <li class="nav-item">
        <a class="nav-link" id="login" asp-area="Identity" asp-page="/Account/Login">Giriş Yap</a>
    </li>
    <li class="nav-item">
        <a class="nav-link" id="register" asp-area="Identity" asp-page="/Account/Register">Kayıt Ol</a>
    </li>
}

